name: Vinted Alert

on:
  schedule:
    - cron: '*/10 * * * *' # Runs every 10 minutes
  workflow_dispatch: # Allows manual trigger

jobs:
  check-vinted:
    runs-on: ubuntu-latest
    timeout-minutes: 15 # Safety timeout
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run Vinted Bot (Loop Mode)
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        # Run for 540 seconds (9 min) with 20s interval
        # This leaves 1 min buffer, then immediately starts next cycle
        # Frequent restarts reset Vinted's detection counter
        run: python vinted_bot_github.py --duration 540 --interval 20

      - name: Commit state (Last Seen ID)
        if: always() # Always run this step even if timeout occurs
        run: |
          git config --global user.name 'Vinted Bot'
          git config --global user.email 'bot@noreply.github.com'
          git add last_seen_id.txt
          if git diff --quiet && git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git stash
            git pull --rebase origin main || {
              # If conflict, take the remote version (it's newer)
              git checkout --theirs last_seen_id.txt
              git add last_seen_id.txt
              git rebase --continue
            }
            git stash pop || true
            git add last_seen_id.txt
            if ! git diff --quiet || ! git diff --staged --quiet; then
              git commit -m "Update last seen ID"
              git push
            fi
          fi
