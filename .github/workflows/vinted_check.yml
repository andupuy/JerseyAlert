name: Vinted Alert

on:
  schedule:
    - cron: '*/15 * * * *' # Runs every 15 minutes
  workflow_dispatch: # Allows manual trigger

jobs:
  check-vinted:
    runs-on: ubuntu-latest
    timeout-minutes: 20 # Safety timeout
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run Vinted Bot (Loop Mode)
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        # Run for 720 seconds (12 min) with 20s interval
        # This leaves 3 min buffer before the next 15-min cycle starts
        # Shorter cycles to avoid Vinted's 20-min detection threshold
        run: python vinted_bot_github.py --duration 720 --interval 20

      - name: Commit state (Last Seen ID)
        if: always() # Always run this step even if timeout occurs
        run: |
          git config --global user.name 'Vinted Bot'
          git config --global user.email 'bot@noreply.github.com'
          git add last_seen_id.txt
          if git diff --quiet && git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git stash
            git pull --rebase origin main || {
              # If conflict, take the remote version (it's newer)
              git checkout --theirs last_seen_id.txt
              git add last_seen_id.txt
              git rebase --continue
            }
            git stash pop || true
            git add last_seen_id.txt
            if ! git diff --quiet || ! git diff --staged --quiet; then
              git commit -m "Update last seen ID"
              git push
            fi
          fi
